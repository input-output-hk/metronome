override {
  metrics {
    enabled = false
  }
  network {
    bootstrap = [
      "localhost:40001",
      "localhost:40002"
    ]
  }
  optional = null
  numeric = 123
  textual = hello world
  boolean = true
}

other {
  metrics {
    enabled = false
  }
}
