override {
  metrics {
    enabled = false
  }
  network {
    bootstrap = [
      "localhost:40001",
      "localhost:40002"
    ]
  }
  optional = null
  numeric = 123
  textual = Hello World
  boolean = true
}

# Other setting that shouldn't be affected.
other {
  metrics {
    enabled = false
  }
}
